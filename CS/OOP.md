# Principles
###  Data hiding (encapsulation)
- Data fields should be private and inaccessible from client application
- Fields should only be accessible through getter and setters
- Other classes should not be able to alter assigned values directly
